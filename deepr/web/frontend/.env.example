# Deepr Web Frontend Configuration
# Copy this file to .env and adjust as needed.

# API base URL — where the frontend sends API requests.
# In development, Vite proxies /api to the Flask backend (see vite.config.ts).
# Use the default unless you need to point at a different backend.
VITE_API_URL=/api
