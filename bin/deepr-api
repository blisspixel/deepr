#!/usr/bin/env python
"""Run Deepr API server."""

import ipaddress
import logging
import os
import sys

from deepr.api.app import app


def _is_loopback(host: str) -> bool:
    if host in ("localhost", ""):
        return True
    try:
        return ipaddress.ip_address(host).is_loopback
    except ValueError:
        return False


def main():
    """Start the API server."""
    # Set up logging
    logging.basicConfig(level=logging.INFO, format="%(asctime)s - %(name)s - %(levelname)s - %(message)s")

    # Get config from environment. Default to loopback for safety -- the API
    # exposes job submission, result retrieval, and provider-backed work that
    # spends operator credentials. Binding to a non-local interface without
    # DEEPR_API_TOKEN is a serious security risk (data disclosure + provider
    # spend abuse). The check below refuses to start in that configuration
    # unless the operator explicitly acknowledges the risk.
    host = os.getenv("API_HOST", "127.0.0.1")
    port = int(os.getenv("API_PORT", 5000))
    debug = os.getenv("FLASK_ENV", "production") == "development"
    api_token_set = bool(os.getenv("DEEPR_API_TOKEN", "").strip())
    allow_unauthenticated_loopback = os.getenv(
        "DEEPR_API_ALLOW_UNAUTHENTICATED_LOOPBACK",
        "",
    ).strip().lower() in ("1", "true", "yes")

    loopback = _is_loopback(host)
    if not api_token_set and (not loopback or not allow_unauthenticated_loopback):
        sys.stderr.write(
            f"ERROR: refusing to start '{host}' without DEEPR_API_TOKEN set. The API\n"
            "exposes job submission and stored reports, and loopback locality is not\n"
            "caller authentication. Set DEEPR_API_TOKEN. For an explicitly accepted\n"
            "loopback-only compatibility mode, set\n"
            "DEEPR_API_ALLOW_UNAUTHENTICATED_LOOPBACK=1.\n"
        )
        sys.exit(2)

    warning = ""
    if not api_token_set:
        warning = "WARNING: explicit unauthenticated loopback compatibility mode."

    print(f"""
===============================================================================

    ########   ##########  ##########  ########      #######
    ##    ##   ##          ##          ##     ##     ##    ##
    ##     ##  ########    ########    ########      ########
    ##    ##   ##          ##          ##            ##    ##
    ########   ##########  ##########  ##            ##     ##

                         API Server
              Deepr Research Automation Platform

===============================================================================

Starting server on {host}:{port}
Debug mode: {debug}
API endpoints: http://{host}:{port}/api
Health check: http://{host}:{port}/api/health
{warning}

Press Ctrl+C to stop
    """)

    try:
        app.run(host=host, port=port, debug=debug)
    except KeyboardInterrupt:
        print("\n\nShutting down API server...")
        sys.exit(0)


if __name__ == "__main__":
    main()
