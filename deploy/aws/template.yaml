# BLOCKED: reference-only legacy AWS template.
# Cloud infrastructure cost cannot be bounded by Deepr's dollar ceiling.
# Historical implementation details remain available in version control.
ReferenceOnly: true
